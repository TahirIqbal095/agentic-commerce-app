# Domain Docs

This is a single-context repository.

Before exploring, read the root `CONTEXT.md` and relevant ADRs under
`docs/adr/` when present. Use the glossary’s defined domain vocabulary and
explicitly flag conflicts with existing ADRs.
